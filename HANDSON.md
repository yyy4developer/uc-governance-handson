# ハンズオン手順 — Unity Catalog のデータガバナンスを自分の手で試す

このハンズオンでは、**Unity Catalog（UC）のガバナンス機能を、実際に手を動かして**確認します。
所要 **1〜2時間**。SQL が書けなくても進められるよう、**画面操作（UI）を中心**に構成しています。

> 💡 **今日つくるもの**: サンプルデータ（`samples.tpch`）を題材に、
> 「メタデータを整える → アクセス制御をかける → 履歴を追う → 探す → 共有する → 監査する → 自然言語で聞く」
> という**ガバナンスの一連の流れ**を、自分のスキーマの中で一通り体験します。

---

## ⏱ 進め方の順番（重要）

**必ず `Part 0` → `Part 1` → `Part 2`… の順で進めてください。**
後ろのノートブックは前のノートブックが作ったテーブルを使うため、飛ばすとエラーになります。

| Part | 内容 | 目安 |
|---|---|---|
| Part 0 | 準備（ログイン・リポジトリ取り込み・自分のスキーマ確認） | 10分 |
| Part 1 | 土台づくり（スキーマ作成 → データ取り込み → メタデータ設計） | 15分 |
| Part 2 | アクセス制御（ABAC：管理タグ + ポリシー） | 20分 |
| Part 3 | データリネージ（どこから来たデータか追う） | 10分 |
| Part 4 | データ探索（欲しいデータを見つける） | 10分 |
| Part 5 | 組織間データ共有（Delta Sharing） | 10分 |
| Part 6 | 監査ログ（誰が何を見たか） | 5分 |
| Part 7 | Genie（自然言語でデータに質問） | 10分 |

⚠️ **Part 2 の最後には「後片付け」のセルがあります。必ず実行してください**（実行しないと Part 3 のテーブルが空になります）。

---

## Part 0. 準備（最初に必ず）

### 0-1. ワークスペースにログイン

講師から共有された URL でワークスペースにログインします。
画面左に **Catalog / Workspace / Genie** などのメニューが見えていればOKです。

### 0-2. このリポジトリを Git folder として取り込む

1. 左メニュー **Workspace** をクリック
2. 自分の **Home** フォルダを開く（`/Users/<自分のメールアドレス>`）
3. 右上の **Create** → **Git folder** をクリック
4. **Git repository URL** に講師から共有された URL を貼り付け
5. **Create Git folder** をクリック

→ Home の下に `uc-governance-handson` フォルダができ、中に `notebooks/core/` が見えます。

### 0-3. ⭐ 自分のスキーマを確認する（最重要）

このハンズオンは**複数人が同じワークスペースで同時に**作業します。
お互いの作業がぶつからないよう、**スキーマは自動で参加者ごとに分けられます**。

1. `notebooks/core/` の **`_config`** ノートブックを開く
2. 右上の **Connect** から **Serverless** を選択（コンピュートに接続）
3. **▶ Run all** をクリック

出力に自分の作業スキーマが表示されます:

```
============================================================
  あなたの作業スキーマ: uc_governance_handson_catalog.uc_handson_taro_yamada
============================================================
```

> ✅ **このスキーマ名をメモしてください**。以降の画面操作で何度も使います。
> ❌ **ファイルを書き換える必要はありません**。カタログ名は講師が設定済みで、
> スキーマ名はログインユーザーから自動で決まります。そのまま実行してください。

### 0-4. SQL Warehouse を確認する

Genie や一部の画面操作で使います。

1. 左メニュー **SQL Warehouses** をクリック
2. 一覧にある Warehouse（例 `Serverless Starter Warehouse`）が **Running** か確認
3. 停止していたら **▶（Start）** をクリック（起動に1〜2分）

---

## Part 1. 土台づくり（スキーマ・データ・メタデータ）

### 1-1. ノートブックを順番に実行する

`notebooks/core/` の中を、**上から順に**実行します。実行は **▶ Run all** ボタンです。

| 順番 | ノートブック | やること |
|---|---|---|
| 1 | `00_setup` | 自分のスキーマと Volume（ファイル置き場）を作る |
| 2 | `01_ingest_data` | `samples.tpch`（標準サンプル）を自分のスキーマに取り込む |
| 3 | `02_catalog_schema` | 説明文（COMMENT）・タグ・主キー/外部キーを整える |

各ノートブックを開いたら:
1. 右上の **Connect** → **Serverless** に接続
2. **▶ Run all** をクリック
3. 全セルに緑のチェックが付いたら次へ

### 1-2. 画面で確認する（UI）

コードで作ったものを、**画面で見て**確認しましょう。

1. 左メニュー **Catalog** をクリック
2. `uc_governance_handson_catalog` を開く
3. **自分のスキーマ**（`uc_handson_<自分の名前>`）を開く
4. テーブル一覧（`customer` / `orders` / `lineitem` など）が見えることを確認
5. `customer` をクリックして以下を確認:
   - **Overview** タブ … 列ごとの説明文（COMMENT）が入っている
   - **Details** タブ … 主キー・外部キーの情報
   - 列名の横の **タグ** … `uc_handson_domain` / `uc_handson_layer` などが付いている

### 1-3. ⭐ AI にコメントを書かせてみる（UI）

説明文をゼロから書くのは大変です。**AI が内容を読んで候補を提案**してくれます。

1. **Catalog** → 自分のスキーマ → `part` テーブルを開く
2. **Overview** タブ右ペインの **AI Suggested Comment** → **AI generate** をクリック
3. 生成された説明文を確認し、**Accept**（採用）または **Edit**（直してから保存）
4. 続いて**列のコメント**も: 列一覧の上の **AI generate** をクリック
   → 列ごとに候補が出るので、良いものを**チェックマーク**で確定

> ⚠️ AI の提案は**必ず内容を確認**してから採用してください（誤った説明が付くと逆効果です）。
> 💡 **なぜ重要か**: 説明文とタグが整っていると、後の「探す」（Part 4）「Genie に聞く」（Part 7）の
> 精度が上がります。ガバナンスの出発点は**メタデータを整えること**、
> そしてその作業は**AI で大幅に省力化できます**。

---

## Part 2. アクセス制御（ABAC：属性ベースアクセス制御）

`notebooks/core/03_access_control` を開いて実行します。

### このパートで体験すること

| # | 内容 | ポイント |
|---|---|---|
| 1 | 階層的な GRANT | カタログ → スキーマ → テーブルで権限が継承される |
| 2 | 管理タグ（Governed Tag）の作成 | UI からも作れる |
| 3 | **Tag Policies（許可値の統制）** | タグの付け間違い・表記ゆれを**システムが防ぐ** |
| 4 | 列へのタグ付与 | タグを付けるだけ（まだデータは変わらない） |
| 5 | **タグ駆動の列マスク** | タグが付いた列を**自動でマスク** |
| 6 | **タグ駆動の行フィルタ** | タグが付いた列で**行を自動で絞る** |
| 7 | 適用中のポリシー一覧 | 何が効いているか一覧で確認 |
| 8 | **後片付け（必ず実行）** | ⚠️ ポリシーを外す。実行しないと Part 3 が空になる |

### 2-1. ノートブックを実行

**▶ Run all** で実行し、出力を確認します。特に注目してほしいのは:

- **「3. Tag Policies」のセル**: 許可していない値（`'ちょっと秘密'`）を入れようとして
  **実際にエラーで弾かれる**様子 → これが組織でタグを統制する仕組みです
- **「5. 列マスク」の後の SELECT**: `c_acctbal`（口座残高）が **NULL になっている**
  → タグを付けた列が自動でマスクされました
- **「6. 行フィルタ」の後の集計**: 行数が絞られている（または 0 件）
  → 自分が対象グループに属していないため

> 💡 **ABAC の威力**: 同じタグを `supplier` の列に付けるだけで、
> **ポリシーを1本も追加せずに**その列もマスクされます（ノートブック内で体験できます）。

### 2-2. 画面で確認する（UI）

**A. 管理タグを見る**
1. 左メニュー **Catalog** → 上部の **Govern**（盾アイコン）
2. **Governed Tags** を選択
3. `uc_handson_pii` をクリック → **許可値**（`confidential` / `restricted`）と**利用状況**を確認

**B. ポリシーを見る**
1. **Catalog** → 自分のスキーマ → **Policies** タブ
2. 作成されたポリシー（列マスク・行フィルタ）が一覧で見える
3. **Create policy** をクリックすると、ポリシー作成画面が開きます
   - **ポリシーの種類**（列マスク / 行フィルター）
   - **範囲（ON）**: カタログ / スキーマ / テーブル
   - **タグ条件**: `uc_handson_pii = confidential` のようにタグで対象列を絞る
   - 右の **コードを表示** で、画面操作と同じ SQL が確認できます
4. 今回は作らずに **キャンセル** でOK（ノートブックで作成済み）

**C. 権限を画面から付与する**
1. `customer` テーブル → **Permissions** タブ → **Grant**
2. **Principals** で付与先、**Privileges** で `SELECT` を選択 → **Grant**

### 2-3. ⚠️ 後片付けを忘れずに

ノートブック末尾の **「8. 後片付け」** のセルを必ず実行してください。
ポリシーを付けたまま次に進むと、`customer` が 0 件・NULL に見え、**Part 3 で作るテーブルが空になります**。

最後の確認セルで **750,000 行・`max_bal` に値** が出ていればOKです。

---

## Part 3. データリネージ（データの来歴を追う）

`notebooks/core/04_lineage` を **▶ Run all**。

マスタとトランザクションを結合して、分析テーブル `order_analysis_summary` を作ります。

### 画面で確認する（UI）

1. **Catalog** → 自分のスキーマ → `order_analysis_summary` をクリック
2. **Lineage** タブをクリック
3. **See lineage graph** をクリック
4. 上流のテーブル（`orders` / `customer` / `nation` / `lineitem`）から
   矢印が伸びている図が表示されます
5. **列レベル**も見られます: 任意の列をクリックすると、その列がどの列から来たか追跡できます

> 💡 **なぜ重要か**: 「この数字はどこから来たのか」を**自動で**追跡できます。
> 影響範囲の調査（このテーブルを変えたら何が壊れるか）にも使えます。

---

## Part 4. データ探索（欲しいデータを見つける）

`notebooks/core/05_discovery` を **▶ Run all**。

### 画面で確認する（UI）

**A. 検索する**
1. 画面上部の **検索ボックス**（または `Ctrl/Cmd + K`）
2. `order_analysis` と入力 → テーブルが候補に出る
3. タグでの絞り込みも可能

**B. 信頼できるデータに印を付ける（Certified）**
1. `order_analysis_summary` テーブルを開く
2. 右上の **⋮（ケバブメニュー）** → **Certify**（または Overview の Certified トグル）
3. 「このテーブルは公式に検証済み」という印が付きます

**C. Insights を見る**
1. テーブルの **Insights** タブ
2. 「よく使われるクエリ」「よく参照する人」などの利用状況が見えます

> 💡 **なぜ重要か**: データが増えると「どれを使えばいいか分からない」問題が起きます。
> タグ・説明文・Certified があると、**信頼できるデータに最短でたどり着けます**。

---

## Part 5. 組織間データ共有（Delta Sharing）

`notebooks/core/06_delta_sharing` を **▶ Run all**。

自分専用の Share（共有セット）が作られます（名前は自動で自分用になります）。

### 画面で確認する（UI）

1. **Catalog** → 上部の **Delta Sharing**（または左メニュー **Delta Sharing**）
2. **Shared by me** タブ → 自分の Share（`order_analysis_share_<自分>`）を確認
3. Share をクリック → **Assets** タブで共有しているテーブルを確認
4. **Recipients** タブ → **Add recipient** で共有先を追加できます
   - **Databricks 同士（D2D）**: 相手の共有識別子を入力
   - **Databricks 以外（D2O）**: **activation link** が発行され、相手はそれでアクセス

> 💡 **なぜ重要か**: データを**コピーせずに**社外・グループ会社と共有できます。
> 共有した後も UC の権限・監査が効き続けます。

---

## Part 6. 監査ログ（誰が何を見たか）

`notebooks/core/07_audit_logs` を **▶ Run all**。

`system.access.audit` から、今日自分がやった操作が見えます。

- 「1. 直近の自分の操作」… GRANT やテーブル作成の記録
- 「3. テーブルアクセスの集計」… 誰がどのテーブルを何回見たか

> ⚠️ 監査ログには **数分〜十数分の遅延**があります。直前の操作が出ないことがあります。
> 💡 **なぜ重要か**: 「機密テーブルに誰がアクセスしたか」を後から証明できます。監査対応の基盤です。

---

## Part 7. Genie（自然言語でデータに質問）

`notebooks/core/08_genie` を **▶ Run all**（対象テーブルの確認と、貼り付け用のテキストが出ます）。

### 画面で作る（UI 主体）

1. 左メニュー **Genie** → **New**（または **＋ New → Genie space**）
2. **Settings → SQL warehouse** で Part 0-4 の Warehouse を選択
3. **Data** タブ → **Add tables** → 自分のスキーマの
   `order_analysis_summary` とマスタ（`customer` / `orders` / `part` / `supplier`）を追加
4. **Instructions** タブ → ノートブックの「2」に出たテキストを貼り付け → **Save**
5. **Sample questions** → ノートブックの「3」の質問を登録
6. 右のチャットで質問してみる:
   - 「市場セグメント別の純売上を多い順に教えて」
   - 「純売上が最も大きい顧客トップ5は？」
7. **Show generated code** で、Genie が書いた SQL を確認
8. ノートブックの「4」で先に検証した SQL の結果と**答え合わせ**

> 💡 **なぜ重要か**: Part 1 で整えた**説明文・タグ・主キー/外部キーが Genie の精度を上げます**。
> ガバナンスは「守り」だけでなく、**AI 活用の土台**でもあります。

---

## トラブルシューティング

| 症状 | 原因と対処 |
|---|---|
| **Part 3 のテーブルが空** | Part 2 の「8. 後片付け」を実行していない → 実行して Part 3 をやり直す |
| **`customer` が 0 行に見える** | 行フィルタが効いている（正常）→ Part 2 の後片付けを実行 |
| **`c_acctbal` が NULL** | 列マスクが効いている（正常）→ Part 2 の後片付けを実行 |
| **管理タグ作成で「既に存在します」** | 他の参加者が先に作成済み（正常）→ そのまま次へ |
| **`is_account_group_member` が全部 false** | 想定どおり。マスク/フィルタが**効く側**として体験できます |
| **ノートブックが実行できない** | 右上 **Connect** で Serverless に接続しているか確認 |
| **他人のテーブルが見えて混乱する** | 自分のスキーマ（`uc_handson_<自分の名前>`）を開いているか確認 |
| **監査ログに何も出ない** | 反映に数分〜十数分かかります。少し待って再実行 |

---

## 参考: Lakehouse Federation（今日は手を動かしません）

ここまでは「Unity Catalog の中にあるデータ」をガバナンスしてきました。
しかし現実には、データは**あちこちに散在**しています。

> 例: 部品マスタは AWS Glue の S3 上、受注実績は Amazon Redshift、
> 別部門は BigQuery や SQL Server、という状態

**Lakehouse Federation** は、これらを**移動・コピーせずに** Unity Catalog 傘下の
カタログとして見せる機能です。

### 何がうれしいか

| | Federation なし | Federation あり |
|---|---|---|
| データを使うには | ETL でコピーして持ってくる | **そのまま参照**（コピー不要） |
| 鮮度 | コピーした時点のもの | **常に最新** |
| ガバナンス | コピー先とコピー元で二重管理 | **UC で一元管理** |
| 権限・マスク・監査 | 個別に設定 | **今日試したものがそのまま効く** |

### today との繋がり

今日体験した機能は、**Federation で取り込んだ外部データにもそのまま適用できます**。

- Part 2 の **ABAC（タグ駆動の行フィルタ・列マスク）** → Glue / Redshift のテーブルにも効く
- Part 3 の **リネージ** → 外部ソースから来たデータの来歴も追跡できる
- Part 6 の **監査ログ** → 外部データへのアクセスも記録される
- Part 7 の **Genie** → 複数ソースを横断した質問に答えられる

### 2つの方式

| 方式 | 対象 | 仕組み |
|---|---|---|
| **Catalog Federation** | AWS Glue / HMS など | メタデータを取り込み、ファイル（Parquet / Delta / Iceberg）を**直接読む** |
| **Query Federation** | Redshift / Snowflake / BigQuery / SQL Server など | クエリを**相手のエンジンに押し込む**（プッシュダウン） |

> ⚠️ **今日ハンズオンで作らない理由**: 接続には AWS 側のリソース（IAM ロール・ネットワーク・
> 権限設定）が必要で、環境構築に相応の準備が必要です。**動作は講師のデモでご覧ください。**
>
> 📖 [Lakehouse Federation](https://docs.databricks.com/ja/query-federation/index.html)
> 💻 実装例はこのリポジトリの `notebooks/federation/` と `terraform/` にあります（参考用）

---

## そのほか今日は扱わないもの

- **Marketplace**: 外部データの調達
- **データ品質モニタリング / 自動 PII 検出**: プロファイル生成に時間がかかるため、必要ならデモで

---

## 参考公式ドキュメント

- [Unity Catalog](https://docs.databricks.com/ja/data-governance/unity-catalog/index.html)
- [ABAC（属性ベースアクセス制御）](https://docs.databricks.com/ja/data-governance/unity-catalog/abac/index.html)
- [管理タグ（Governed Tags）](https://docs.databricks.com/ja/admin/governed-tags/)
- [データリネージ](https://docs.databricks.com/ja/data-governance/unity-catalog/data-lineage.html)
- [Delta Sharing](https://docs.databricks.com/ja/delta-sharing/index.html)
- [監査ログ system table](https://docs.databricks.com/ja/admin/system-tables/audit-logs.html)
- [AI/BI Genie](https://docs.databricks.com/ja/genie/index.html)
